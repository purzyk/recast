# Copy to .env for local development. The real .env is gitignored.
# Matches the credentials in docker-compose.yml.
DATABASE_URL="postgresql://recast:recast@localhost:5432/recast"

# Tailoring (Phase 9). Billed per token, separately from any Claude subscription.
ANTHROPIC_API_KEY=""
# Optional. Defaults to claude-opus-5.
# RECAST_MODEL="claude-opus-5"
